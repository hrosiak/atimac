Requirements
------------
1. fortran ATIMA v1.3 sources in atima.1.3 
2. cython for python module 

Compiling
----------
1. make
2. make py (optional for building python module)

Usage
------
see example folder

REST server
-----------

Flask atimac server is available in server directory. It requires atimac python module and flask python module.
CPPCMS C++ version of the REST API server is available as well, however flask version is recommended.